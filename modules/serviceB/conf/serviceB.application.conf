# This file is only used when the serviceB SBT subproject runs standalone provided that build.sbt contains a line like:
# javaOptions += "-Dconfig.resource=serviceB.application.conf"

include "application.conf"

play.http.router = serviceB.Routes

# Configuration settings specific to the serviceB project:
